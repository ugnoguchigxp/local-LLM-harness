# AMD AI Max 395+ (128GB RAM) 向けローカルLLM環境構築計画

この計画は、AMD Ryzen AI Max 395+（128GB RAM搭載）のハイスペックPCをローカルLLM実行専用サーバーとして最適化するための環境構築手順をまとめたものです。
メモリ容量128GBと広帯域なメモリバスを活かし、**Qwen 2.5 72B** や **Gemma 2 27B** などの大型モデルをOpenAI API互換（ストリーミング対応）でバックグラウンド実行（デーモン化）できるようにします。

---

## 提案する2つのアプローチ

本構成では、安定性と導入の容易さを重視した **プランA (Ollama方式)** を推奨しますが、より細かなチューニングや最大パフォーマンスを求める場合の **プランB (llama.cpp方式)** も提示します。

### プランA：Ollama による構築（推奨・最も簡単）
OllamaのWindows版を使用します。Ollamaは自動的にAMD GPU (ROCm/DirectML/Vulkan) を検出し、最適にモデルを配置します。

- **メリット**: 
  - インストールが極めて簡単。
  - 最初からOpenAI API互換エンドポイント (`http://localhost:11434/v1`) が動作。
  - ストリーミングに標準対応。
  - Windows起動時の自動起動（常駐タスクトレイアプリ）が標準機能として提供されている。
  - モデルのダウンロードと更新が `ollama run` コマンド一発で完了する。

### プランB：llama.cpp (Vulkan/ROCm) + NSSM による構築（パフォーマンス追求）
`llama-server.exe` を直接使用し、NSSM（Non-Sucking Service Manager）を使ってWindowsサービス化します。

- **メリット**:
  - Vulkanバックエンドを明示的に指定することで、Ryzen AI Max 395+のGPUパワーを限界まで引き出せる。
  - コンテキストサイズやCPUスレッド数、GPUオフロードの配分を極めて細かく調整可能。
- **デメリット**:
  - GGUFモデルの個別ダウンロードや、サービス登録のためのコマンド実行など、手順がやや複雑。

---

## ユーザー確認・検討事項

> [!IMPORTANT]
> どちらのプランを採用するかご意見をお聞かせください。基本的には、まずは手軽でアップデートも容易な **プランA (Ollama)** で環境を構築し、パフォーマンスに不満がある場合に **プランB (llama.cpp)** へ移行する形を推奨いたします。
>
> また、以下の設定項目についてご希望があればお知らせください：
> 1. **外部アクセス**: ローカルPC内からのみAPIを叩くか、それとも同じLAN内の別のPCやスマホからもアクセスできるようにするか（APIサーバーのリスンアドレスを `localhost` にするか `0.0.0.0` にするか）。
> 2. **モデル保存先**: 128GB RAMで大型モデルを扱う場合、Cドライブの容量を多く消費します。モデル保存先を別の高速なNVMe SSD（Dドライブなど）に変更したいですか？

---

## 提案する具体的な構成内容

### 1. 導入モデルの選定（128GB RAMに最適化）
128GBのRAMおよび広帯域AMD APUメモリバスを活かし、最新の **Qwen 3.8 27B** を主力モデルとして採用します。

- **Qwen 3.8 27B (Q4_K_M + MTP)**: 
  - 高度な日本語能力、思考モード (Thinking Mode)、関数呼び出し (Tool Calling)、マルチモーダルに対応。
  - **超軽量KVキャッシュ (160k Context 運用)**: 160,000 (160k) トークン（`--ctx-size 163840`）をターゲットとし、`-ctk q4_0 -ctv q4_0` 設定により KV キャッシュ占有量はわずか約 5.0 GB に抑制。
  - `Q4_K_M` (4bit量子化, 約17GB) と MTP (Multi-Token Prediction / NextN 並行推論) を組み合わせることで、単一カード超の高速トークン生成を実現。
  - デュアルインスタンス構成（2台同時起動）でも合計メモリ消費量は約 47 GB に収まり、128GB RAM環境で圧倒的な余裕（80GB以上の空き）をもって安定動作します。
- **Gemma 2 27B (q4_K_M または q8_0)**:
  - Google製。サイズに対して非常に賢く、日本語や英語の論理的思考に優れています。
  - 27Bサイズ (`q4_K_M` で約19GB, `q8_0` で約30GB) はAMD APUのメモリ帯域を活かして非常に高速（高トークン/秒）で動作します。

### 2. デーモン化（バックグラウンド実行）の設計
- **Ollamaの場合**: Windowsのスタートアップフォルダにショートカットを登録するか、Ollama自体の常駐設定を利用します。さらに、環境変数（`OLLAMA_HOST` 等）を設定してバックグラウンドでリスンさせます。
- **llama.cppの場合**: `llama-server.exe` を起動するバッチファイルを作成し、NSSMを用いてWindowsサービスとして登録します。これにより、ユーザーがログインしていなくてもPCの電源が入れば自動的にAPIサーバーが起動します。

---

## 変更予定ファイル・作成ファイル一覧

環境構築をサポートするために、以下のヘルパースクリプトおよび設定ファイルを新規作成します。

#### [NEW] [plan.md](file:///C:/Users/yuji/local-llm-setup/plan.md) (本ファイル)
#### [NEW] [setup_helper.ps1](file:///C:/Users/yuji/local-llm-setup/setup_helper.ps1)
Ollamaのインストール状況確認、環境変数の設定（ホスト設定や保存先変更）、推奨モデルの自動プル、API疎通確認（ストリーミング対応テスト）を一括で行うPowerShellスクリプト。

#### [NEW] [test_openai_api.py](file:///C:/Users/yuji/local-llm-setup/test_openai_api.py)
OpenAI API互換でストリーミングが正しく動作するか検証するためのPythonテストスクリプト。

#### [NEW] [install_service_llamacpp.ps1](file:///C:/Users/yuji/local-llm-setup/install_service_llamacpp.ps1) (プランB選択時のみ使用)
llama.cppとNSSMをダウンロードし、Windowsサービスとして登録するための自動セットアップスクリプト。

---

## 検証プラン

### 1. 動作確認テスト
- `setup_helper.ps1` を通じて API サーバーの起動を確認。
- `test_openai_api.py` を動かし、OpenAI互換APIエンドポイントから文字がストリーミング形式でリアルタイムに返ってくることを確認。

### 2. 負荷・メモリ確認
- タスクマネージャーにて、メモリ使用量とGPU/NPUの動作状況を確認（AMD GPUの共有メモリや専用VRAMに適切に配置されているか確認）。
