diff --git a/api/backends/optimized_backend.py b/api/backends/optimized_backend.py
index 95db28e..fb7bbd6 100644
--- a/api/backends/optimized_backend.py
+++ b/api/backends/optimized_backend.py
@@ -12,7 +12,48 @@ import numpy as np
 
 logger = logging.getLogger(__name__)
 
-CONFIG_PATH = Path.home() / "qwen3-tts" / "config.yaml"
+CONFIG_PATH = Path(
+    os.getenv("TTS_CONFIG", str(Path.home() / "qwen3-tts" / "config.yaml"))
+).expanduser()
+
+
+def install_rocm_multinomial_fallback() -> None:
+    """Sample on CPU to avoid the gfx1151 HIP multinomial failure."""
+    import torch
+
+    if not torch.version.hip or getattr(torch.multinomial, "_gfx1151_fallback", False):
+        return
+
+    native_multinomial = torch.multinomial
+
+    def cpu_multinomial(
+        probabilities,
+        num_samples,
+        replacement=False,
+        *,
+        generator=None,
+        out=None,
+    ):
+        if probabilities.device.type != "cuda":
+            return native_multinomial(
+                probabilities,
+                num_samples,
+                replacement=replacement,
+                generator=generator,
+                out=out,
+            )
+        sampled = native_multinomial(
+            probabilities.float().cpu(),
+            num_samples,
+            replacement=replacement,
+        ).to(probabilities.device)
+        if out is not None:
+            out.copy_(sampled)
+            return out
+        return sampled
+
+    cpu_multinomial._gfx1151_fallback = True
+    torch.multinomial = cpu_multinomial
 
 def load_config() -> dict:
     if CONFIG_PATH.exists():
@@ -23,8 +64,9 @@ def load_config() -> dict:
 # Model mapping: OpenAI model name -> our model key
 MODEL_MAPPING = {
     "tts-1": "0.6B-CustomVoice",
     "tts-1-hd": "1.7B-CustomVoice",
     "qwen3-tts": "0.6B-CustomVoice",
+    "qwen3-tts-expressive": "0.6B-CustomVoice",
     "qwen3-tts-hd": "1.7B-CustomVoice",
 }
 
@@ -59,10 +101,26 @@ class OptimizedQwen3TTSBackend:
     def get_current_model_key(self) -> Optional[str]:
         return self.current_model_key
 
+    def _generation_kwargs(self) -> dict:
+        allowed = {
+            "do_sample",
+            "top_k",
+            "top_p",
+            "temperature",
+            "subtalker_dosample",
+            "subtalker_top_k",
+            "subtalker_top_p",
+            "subtalker_temperature",
+        }
+        configured = self.config.get("generation", {})
+        return {key: value for key, value in configured.items() if key in allowed}
+
     async def _ensure_model_loaded(self, model_key: str) -> None:
         """Load model if not already loaded."""
         import torch
 
+        install_rocm_multinomial_fallback()
+
         if self.current_model_key == model_key and self.model is not None:
             return
 
@@ -121,7 +179,6 @@ class OptimizedQwen3TTSBackend:
                     compile_mode=opt.get("compile_mode", "max-autotune"),
                     use_fast_codebook=opt.get("use_fast_codebook", True),
                     compile_codebook_predictor=opt.get("compile_codebook_predictor", True),
-                    compile_talker=opt.get("compile_talker", False),
                 )
                 logger.info(f"torch.compile enabled: mode={opt.get('compile_mode', 'max-autotune')}, fast_codebook={opt.get('use_fast_codebook', True)}")
 
@@ -256,6 +313,7 @@ class OptimizedQwen3TTSBackend:
             language=language,
             speaker=voice,
             instruct=instruct,
+            **self._generation_kwargs(),
         )
 
         audio = wavs[0]
@@ -304,5 +362,6 @@ class OptimizedQwen3TTSBackend:
             instruct=instruct,
             emit_every_frames=emit_every_frames,
             decode_window_frames=decode_window_frames,
+            **self._generation_kwargs(),
         ):
             yield chunk, sr
diff --git a/api/main.py b/api/main.py
index a0fab6c..3f2e728 100644
--- a/api/main.py
+++ b/api/main.py
@@ -41,6 +41,7 @@ WORKERS = int(os.getenv("WORKERS", "1"))
 # Backend configuration
 TTS_BACKEND = os.getenv("TTS_BACKEND", "official")
 TTS_WARMUP_ON_START = os.getenv("TTS_WARMUP_ON_START", "false").lower() == "true"
+TTS_PRELOAD_ON_START = os.getenv("TTS_PRELOAD_ON_START", "true").lower() == "true"
 GPU_KEEPALIVE_INTERVAL = int(os.getenv("GPU_KEEPALIVE_INTERVAL", "0"))
 
 # CORS configuration
@@ -82,21 +83,25 @@ async def lifespan(app: FastAPI):
         logger.info(f"Voice Studio: http://{display_host}:{PORT}/voice-studio")
     logger.info(boundary)
 
-    # Pre-load the TTS backend
-    try:
-        from .backends import initialize_backend
-        logger.info(f"Initializing TTS backend: {TTS_BACKEND}")
-        backend = await initialize_backend(warmup=TTS_WARMUP_ON_START)
-        logger.info(f"TTS backend '{backend.get_backend_name()}' loaded successfully!")
-        logger.info(f"Model: {backend.get_model_id()}")
-
-        device_info = backend.get_device_info()
-        if device_info.get("gpu_available"):
-            logger.info(f"GPU: {device_info.get('gpu_name')}")
-            logger.info(f"VRAM: {device_info.get('vram_total')}")
-    except Exception as e:
-        logger.warning(f"Backend initialization delayed: {e}")
-        logger.info("Backend will be loaded on first request.")
+    # Compiled ROCm providers may need minutes to warm up. Allow metadata
+    # endpoints to become available first and initialize on first synthesis.
+    if TTS_PRELOAD_ON_START:
+        try:
+            from .backends import initialize_backend
+            logger.info(f"Initializing TTS backend: {TTS_BACKEND}")
+            backend = await initialize_backend(warmup=TTS_WARMUP_ON_START)
+            logger.info(f"TTS backend '{backend.get_backend_name()}' loaded successfully!")
+            logger.info(f"Model: {backend.get_model_id()}")
+
+            device_info = backend.get_device_info()
+            if device_info.get("gpu_available"):
+                logger.info(f"GPU: {device_info.get('gpu_name')}")
+                logger.info(f"VRAM: {device_info.get('vram_total')}")
+        except Exception as e:
+            logger.warning(f"Backend initialization delayed: {e}")
+            logger.info("Backend will be loaded on first request.")
+    else:
+        logger.info("Backend preload disabled; synthesis initializes the model on demand.")
 
     # GPU keepalive: periodic matmul to prevent AMD DPM idle downclocking
     keepalive_task = None
diff --git a/api/routers/openai_compatible.py b/api/routers/openai_compatible.py
index 056c6fd..0000000 100644
--- a/api/routers/openai_compatible.py
+++ b/api/routers/openai_compatible.py
@@ -147,8 +147,9 @@ for lang_code in LANGUAGE_CODE_MAPPING.keys():
 # Model name mapping (OpenAI -> internal)
 MODEL_MAPPING = {
     "tts-1": "qwen3-tts",
     "tts-1-hd": "qwen3-tts",
     "qwen3-tts": "qwen3-tts",
+    "qwen3-tts-expressive": "qwen3-tts",
 }
 
 # Add language-specific model mappings
@@ -566,7 +567,10 @@ async def list_voices():
     default_languages = ["English", "Chinese", "Japanese", "Korean", "German", "French", "Spanish", "Russian", "Portuguese", "Italian"]
 
     try:
-        backend = await get_tts_backend()
+        # Voice metadata is configuration-only. Do not trigger multi-minute
+        # model compilation when this is the first request after a cold start.
+        from ..backends import get_backend
+        backend = get_backend()
         speakers = backend.get_supported_voices()
         languages = backend.get_supported_languages()
 
